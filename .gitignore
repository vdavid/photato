# Dependencies
node_modules/
jspm_packages/
.pnpm-store/

# Build outputs
dist/

# Env / secrets
.env
.env.*
!.env.example
# Materialized at deploy time from /etc/photato-deploy.env; never committed.
infra/photato-secrets.env

# Legacy Serverless (backend/, being replaced by Go)
.serverless/
serverless_sdk/
s_*.js

# Editors
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Personal Claude config (shared config lives in .claude/settings.json)
.claude/settings.local.json
.claude/hooks/session-start.sh
# Claude agent workflow state — auto-created when the agent runs, not source of truth
.claude/scheduled_tasks.lock
.claude/worktrees/
